{
  // Allow authenticated users to read data, but only authenticated content owners can
  // make changes to their data
  "rules": {
    ".read": "auth.uid !== null",
    ".write": false,
    "database": {
      "users": {
        "$uid": {
          ".read": "auth.uid !== null",
          ".write": "auth.uid === $uid"
        }
      },
      "spaceInvaders": {
        "$uid": {
          ".read": "auth.uid !== null",
          ".write": "auth.uid === $uid"
        }
      },
      "geoDash": {
        "$uid": {
          ".read": "auth.uid !== null",
          ".write": "auth.uid === $uid"
        }
      }
    }
  }
}